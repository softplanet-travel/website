document.addEventListener("DOMContentLoaded",()=>{const root=document.getElementById("toolList"),catalog=window.SoftPlanetServices.blockCatalog;function render(term=""){const filtered=catalog.filter(item=>`${item.name}${item.note}`.includes(term));root.innerHTML=filtered.length?filtered.map(item=>`<article><span>${item.icon}</span><div><h3>${item.name}</h3><p>${item.note}</p></div><a class="secondary-link" href="trips.html">選擇旅行</a></article>`).join(""):`<div class="soft-empty compact-empty"><span>🐻</span><h3>沒有找到符合「${term}」的工具</h3><p>工具只能從正式旅行積木中選擇。</p></div>`}window.SoftPlanetSearch.mount(document.getElementById("toolSearch"),{placeholder:"搜尋旅行積木",onSearch:render});render()});
